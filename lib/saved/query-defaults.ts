import type { AppLocale } from "@/lib/types";

export const SAVED_ITEMS_LIMIT_MIN = 1;
export const SAVED_ITEMS_LIMIT_MAX = 200;
export const DEFAULT_SAVED_ITEMS_LIMIT = 80;
export const DEFAULT_SAVED_ITEMS_LOCALE = "en" satisfies AppLocale;
